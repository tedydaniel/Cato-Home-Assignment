"use client";import{Shell,Conversations,Composer,Messages}from"../components";import{useApp}from"../providers";
export default function Chat(){const{convs,selected}=useApp();const c=convs.find(x=>x.id===selected);return <Shell><main className="chat-layout"><Conversations/><section className="chat"><header><div><p className="eyebrow">Customer support</p><h1>{c?.title??"No conversation selected"}</h1><span>{c?.customer} · {c?.site}</span></div><span className="pill">{c?.status}</span></header><div className="thread">{c&&<Messages conversation={c}/>}</div><Composer/></section></main></Shell>}
