"use client";
import Link from "next/link";import{usePathname}from"next/navigation";import{useApp,Conv}from"./providers";import{FormEvent,useState}from"react";
export function Shell({children}:{children:React.ReactNode}){const p=usePathname();return <div className="shell"><aside><div className="brand"><span>◈</span> Cato<span className="muted">Support AI</span></div><nav>{[["/chat","◉","Chat"],["/debug","⌘","Debug chat"],["/tasks","⚑","Tasks & alerts"]].map(([href,i,label])=><Link className={p===href?"active":""} href={href} key={href}>{i} {label}</Link>)}</nav><div className="side-note"><b>System status</b><span>● All systems operational</span></div></aside>{children}</div>}
export function Conversations(){const{convs,selected,select,add,remove}=useApp();return <section className="conversations"><div className="section-head"><b>Conversations</b><button onClick={add}>＋ New</button></div>{convs.map(c=><div className={c.id===selected?"conv selected":"conv"} key={c.id} onClick={()=>select(c.id)}><div><b>{c.title}</b><small>{c.customer}</small></div><button aria-label="Delete conversation" onClick={e=>{e.stopPropagation();remove(c.id)}}>×</button></div>)}</section>}
export function Composer(){const{send}=useApp();const[text,setText]=useState("");function submit(e:FormEvent){e.preventDefault();if(text.trim()){send(text.trim());setText("")}}return <form className="composer" onSubmit={submit}><input value={text} onChange={e=>setText(e.target.value)} placeholder="Describe your issue or ask a question…"/><button>Send ↑</button></form>}
export function Messages({conversation}:{conversation:Conv}){return <>{conversation.messages.length?conversation.messages.map((m,i)=><div className={`message ${m.role}`} key={i}><div className="avatar">{m.role==="agent"?"◈":"C"}</div><div><b>{m.role==="agent"?"Cato Support AI":"Customer"}</b><p>{m.text}</p><small>{m.time}</small></div></div>):<div className="empty">Start the conversation. The agent will identify the customer before troubleshooting.</div>}</>}
